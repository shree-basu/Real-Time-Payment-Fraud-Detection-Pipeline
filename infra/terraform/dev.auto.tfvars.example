# Copy only for an intentionally authorized deployment. Defaults remain non-deploying.
# Once applied, keep both gate values unchanged until a separately reviewed teardown.
project_id              = "replace-with-explicit-project-id"
region                  = "us-central1"
bigquery_location       = "US"
deployment_enabled      = false
deployment_confirmation = ""
enable_monitoring       = false
